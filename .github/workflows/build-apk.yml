# Repo kokunde VEYA SwitchTranslator/ alt klasorunde proje olabilir (manuel yukleme).

name: Build debug APK

on:
  workflow_dispatch:
  push:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
          cache-dependency-path: "**/gradle/wrapper/gradle-wrapper.properties"

      - name: Android SDK
        uses: android-actions/setup-android@v4
        with:
          packages: "platforms;android-34 build-tools;34.0.0"

      # Web yuklemede klasor adi farkli olabilir (swtich/swtch/swtych); gradlew + app/ olan dizini bul.
      - name: Proje klasorunu bul
        id: proj
        shell: bash
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          GRADLEW=""
          while IFS= read -r line; do
            d="$(dirname "$line")"
            if [ -f "$d/app/build.gradle.kts" ] || [ -f "$d/app/build.gradle" ]; then
              GRADLEW="$line"
              break
            fi
          done < <(find . -name gradlew -type f ! -path './.git/*' 2>/dev/null || true)
          if [ -z "$GRADLEW" ]; then
            GRADLEW=$(find . -name gradlew -type f ! -path './.git/*' 2>/dev/null | head -n1 || true)
          fi
          if [ -z "$GRADLEW" ]; then
            echo "::error::gradlew bulunamadi. Yukleme kokunde veya alt klasorde gradlew + app/ olmali."
            echo "Depo ozeti (ilk 120 satir):"
            find . -maxdepth 8 \( -type f -o -type d \) 2>/dev/null | head -120
            exit 1
          fi
          DIR="$(dirname "$GRADLEW")"
          REL="$(realpath --relative-to="$GITHUB_WORKSPACE" "$DIR")"
          echo "dir=$REL" >> "$GITHUB_OUTPUT"
          echo "Gradle proje dizini: $REL"
          java -version
          echo "JAVA_HOME=$JAVA_HOME"

      # Eski push'larda Windows-ROOT SSL satirlari Linux JVM'de SSLContext hatasi verir; CI'da temizle.
      - name: gradle.properties CI (SSL)
        working-directory: ${{ steps.proj.outputs.dir }}
        run: |
          if [ -f gradle.properties ]; then
            sed -i '/^systemProp\.javax\.net\.ssl\.trustStoreType=Windows-ROOT/d' gradle.properties || true
            sed -i 's/ -Djavax\.net\.ssl\.trustStoreType=Windows-ROOT//g' gradle.properties || true
          fi

      # Eksik yuklemede jar olmayabilir; Gradle 8.2 ile uyumlu jar indirilir.
      - name: Gradle wrapper jar
        working-directory: ${{ steps.proj.outputs.dir }}
        run: |
          mkdir -p gradle/wrapper
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "::notice::gradle-wrapper.jar yoktu; CI indiriyor."
            curl -fsSL -o gradle/wrapper/gradle-wrapper.jar \
              "https://github.com/gradle/gradle/raw/v8.2.0/gradle/wrapper/gradle-wrapper.jar"
          fi
          ls -la gradle/wrapper/
          test -f gradle/wrapper/gradle-wrapper.jar || { echo "::error::gradle-wrapper.jar alinamadi."; exit 1; }

      - name: gradlew hazirla
        working-directory: ${{ steps.proj.outputs.dir }}
        run: |
          sed -i 's/\r$//' gradlew || true
          chmod +x gradlew

      - name: Derle
        working-directory: ${{ steps.proj.outputs.dir }}
        env:
          # CI runner'da toolchain ile ek JDK indirme / bellek sınırı sorunlarını azaltır
          ORG_GRADLE_JAVA_INSTALLATIONS_AUTO_DOWNLOAD: "false"
          GRADLE_OPTS: "-Xmx4096m -Dfile.encoding=UTF-8"
          _JAVA_OPTIONS: "-Xmx4096m"
        run: ./gradlew assembleDebug --no-daemon --stacktrace --no-parallel

      - name: APK dosya yolu
        id: apk
        run: |
          D="${{ steps.proj.outputs.dir }}"
          if [ "$D" = "." ]; then echo "path=app/build/outputs/apk/debug/*.apk" >> $GITHUB_OUTPUT
          else echo "path=$D/app/build/outputs/apk/debug/*.apk" >> $GITHUB_OUTPUT
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: SwitchTranslator-debug
          path: ${{ steps.apk.outputs.path }}
